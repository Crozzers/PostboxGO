package com.crozzers.postboxgo.utils

/**
 * Maximum distance in metres a person can be from a postbox for that postbox to be considered
 * "verified" - IE: the person was actually there
 */
const val MAX_POSTBOX_VERIFIED_DISTANCE = 3000